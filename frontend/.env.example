# Base URL for the backend API.
# Leave empty in development — Vite will proxy /api to http://localhost:8000 automatically.
# Set this in production to the full URL of your deployed backend, e.g.:
#   VITE_API_BASE=https://api.example.com
VITE_API_BASE=
